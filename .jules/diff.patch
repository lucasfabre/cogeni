diff --git a/Justfile b/Justfile
index 6fd84be..0020a08 100644
--- a/Justfile
+++ b/Justfile
@@ -159,7 +159,8 @@ test: build
 	go test ./...
 	./tests/shell/framework/run_all_tests.sh

-build-docs:
+build-docs: build
+	./cogeni run cogeni.lua
 	python3 ./scripts/build_man_pages_md.py
 	./scripts/build_api_docs_md.sh
 	./scripts/build_site.sh
diff --git a/cogeni.lua b/cogeni.lua
new file mode 100644
index 0000000..8d8dafa
--- /dev/null
+++ b/cogeni.lua
@@ -0,0 +1,227 @@
+-- cogeni.lua
+
+-- Static content for the man page
+local MAN_HEADER = [[
+.TH COGENI-LUA 7 "February 2026" "0.1.0" "cogeni Manual"
+.SH NAME
+cogeni-lua \- Lua API reference and patterns for cogeni
+.SH DESCRIPTION
+The \fBcogeni\fR environment provides a suite of modules designed for AST-driven code generation.
+.SH GLOBAL VARIABLES
+.TP
+.B _CURRENT_FILE
+Absolute path to the currently executing Lua script.
+.TP
+.B _FILE_EXTENSION
+Extension of the currently executing script (including the dot).
+]]
+
+local MAN_FOOTER = [[
+.SH EXAMPLES
+.SS 1. Basic Generation
+.nf
+    # <cogeni>
+    #   write("hello", "print('hello world')")
+    # </cogeni>
+    # <generated by="cogeni" id="hello">
+    # </cogeni>
+.fi
+.SS 2. Multi-file Orchestration
+.nf
+    -- cogeni.lua
+    local files = fs.find(".", { type = "f", name = "*.py" })
+    for path, _ in pairs(files) do
+        async(cogeni.process, path)
+    end
+.fi
+.SH AST TABLE SCHEMA
+Every node in the AST is a table:
+.TP
+.B type
+Grammar-defined node name (e.g., "identifier").
+.TP
+.B fields
+Map of named children (e.g., \fBast.fields.name\fR).
+.TP
+.B children
+Ordered list of all sub-nodes.
+.TP
+.B content
+Raw source text (typically only for leaf nodes).
+.TP
+.B start_pos, end_pos
+Coordinate pairs \fB{row, col}\fR.
+.SH BEST PRACTICES
+.IP \(bu 2
+\fBPath Resolution\fR: Always use \fBfs.join(fs.basedir(_CURRENT_FILE), ...)\fR for relative paths.
+.IP \(bu 2
+\fBConcurrency\fR: Use \fBasync\fR for I/O bound tasks like \fBcogeni.process\fR.
+.SH SEE ALSO
+.BR cogeni (1)
+]]
+
+-- Helper function to parse a doc block
+local function parse_doc_block(lines)
+    local doc = {
+        module = "",
+        func = "",
+        summary = "",
+        usage = "",
+        params = {},
+        returns = ""
+    }
+
+    for _, line in ipairs(lines) do
+        -- Strip leading // or spaces
+        line = line:gsub("^%s*//%s*", ""):gsub("^%s*", "")
+
+        if line:find("@module") then
+            doc.module = line:match("@module%s+(.+)")
+        elseif line:find("@function") then
+            doc.func = line:match("@function%s+(.+)")
+        elseif line:find("@summary") then
+            doc.summary = line:match("@summary%s+(.+)")
+        elseif line:find("@usage") then
+            doc.usage = line:match("@usage%s+(.+)")
+        elseif line:find("@param") then
+            local p_name, p_type, p_desc = line:match("@param%s+(%S+)%s+(%S+)%s+(.+)")
+            if not p_name then
+                 p_name, p_type = line:match("@param%s+(%S+)%s+(%S+)")
+                 p_desc = ""
+            end
+            if not p_name then
+                 p_name, p_type, p_desc = line:match("@param%s+(%.%.%.)%s+(%S+)%s+(.+)")
+            end
+
+            if p_name then
+                table.insert(doc.params, { name = p_name, type = p_type, desc = p_desc })
+            else
+                -- Fallback
+                local rest = line:match("@param%s+(.+)")
+                table.insert(doc.params, { name = "...", type = "any", desc = rest })
+            end
+        elseif line:find("@returns") then
+            doc.returns = line:match("@returns%s+(.+)")
+        end
+    end
+    return doc
+end
+
+-- Collect docs
+local docs_by_module = {}
+
+local files = fs.find("src/lua_runtime", { type = "f", name = "*.go" })
+
+for path, _ in pairs(files) do
+    local ast = cogeni.read_ast(path, "go")
+    local comments = jq.query(ast, '.. | select(.type? == "comment")')
+    if type(comments) ~= "table" then comments = { comments } end
+
+    local in_block = false
+    local current_block = {}
+
+    for _, comment in ipairs(comments) do
+        local content = comment.content
+        if content:find("<lua_api>") then
+            in_block = true
+            current_block = {}
+        elseif content:find("</lua_api>") then
+            in_block = false
+            local doc = parse_doc_block(current_block)
+            if doc.module ~= "" then
+                if not docs_by_module[doc.module] then
+                    docs_by_module[doc.module] = {}
+                end
+                table.insert(docs_by_module[doc.module], doc)
+            end
+        elseif in_block then
+            table.insert(current_block, content)
+        end
+    end
+end
+
+-- Generate output
+local output = {}
+table.insert(output, MAN_HEADER)
+
+-- Helper to render details
+local function render_details(doc)
+    if #doc.params > 0 then
+        table.insert(output, ".IP")
+        table.insert(output, "\\fBParameters:\\fR")
+        table.insert(output, ".RS")
+        for _, p in ipairs(doc.params) do
+            local desc = p.desc or ""
+            table.insert(output, "\\fI" .. p.name .. "\\fR (" .. p.type .. "): " .. desc)
+            table.insert(output, ".br")
+        end
+        table.insert(output, ".RE")
+    end
+
+    if doc.returns and doc.returns ~= "" then
+        table.insert(output, ".IP")
+        table.insert(output, "\\fBReturns:\\fR")
+        table.insert(output, ".RS")
+        table.insert(output, doc.returns)
+        table.insert(output, ".RE")
+    end
+end
+
+-- Global Functions
+if docs_by_module["global"] then
+    table.insert(output, ".SH GLOBAL FUNCTIONS")
+    -- Sort functions
+    table.sort(docs_by_module["global"], function(a, b) return a.func < b.func end)
+
+    for _, doc in ipairs(docs_by_module["global"]) do
+        table.insert(output, ".TP")
+        if doc.usage and doc.usage ~= "" then
+             table.insert(output, ".B " .. doc.usage)
+        else
+             table.insert(output, ".B " .. doc.func)
+        end
+
+        if doc.summary and doc.summary ~= "" then
+            table.insert(output, doc.summary)
+        end
+        render_details(doc)
+    end
+end
+
+-- Modules
+table.insert(output, ".SH MODULES")
+
+-- Sort module names
+local modules = {}
+for mod, _ in pairs(docs_by_module) do
+    if mod ~= "global" then
+        table.insert(modules, mod)
+    end
+end
+table.sort(modules)
+
+for _, mod in ipairs(modules) do
+    table.insert(output, ".SS " .. mod)
+    local funcs = docs_by_module[mod]
+    table.sort(funcs, function(a, b) return a.func < b.func end)
+
+    for _, doc in ipairs(funcs) do
+        table.insert(output, ".TP")
+        if doc.usage and doc.usage ~= "" then
+             table.insert(output, ".B " .. doc.usage)
+        else
+             table.insert(output, ".B " .. mod .. "." .. doc.func)
+        end
+
+        if doc.summary then
+            table.insert(output, doc.summary)
+        end
+        render_details(doc)
+    end
+end
+
+table.insert(output, MAN_FOOTER)
+
+local content = table.concat(output, "\n")
+cogeni.outfile("lua_man", "docs/man/man7/cogeni-lua.7")
+write("lua_man", content)
diff --git a/docs/docusaurus/docs/man/_category_.json b/docs/docusaurus/docs/man/_category_.json
index 474c0ee..c4948a7 100644
--- a/docs/docusaurus/docs/man/_category_.json
+++ b/docs/docusaurus/docs/man/_category_.json
@@ -1 +1 @@
-{"label": "Man Pages", "position": 4}
+{"label": "Man Pages", "position": 4}
\ No newline at end of file
diff --git a/docs/man/man7/cogeni-lua.7 b/docs/man/man7/cogeni-lua.7
index 69764fd..a6c9b66 100644
--- a/docs/man/man7/cogeni-lua.7
+++ b/docs/man/man7/cogeni-lua.7
@@ -10,69 +10,264 @@ Absolute path to the currently executing Lua script.
 .TP
 .B _FILE_EXTENSION
 Extension of the currently executing script (including the dot).
+
 .SH GLOBAL FUNCTIONS
 .TP
-.B write(id, content)
-Buffers text for a specific output block. By default, targets \fB<generated id="ID">\fR in the current file.
-.TP
 .B async(fn, ...)
-Starts a new asynchronous task (coroutine). Returns a job handle.
+Starts a new asynchronous task.
+.IP
+\fBParameters:\fR
+.RS
+\fIfn\fR (function): The function to run asynchronously.
+.br
+\fI...\fR (any): Arguments to pass to the function.
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+userdata A job handle.
+.RE
 .TP
-.B await(job_handle)
-Pauses the current task until the specified job completes.
+.B await(job)
+Pauses until a job completes.
+.IP
+\fBParameters:\fR
+.RS
+\fIjob\fR (userdata): The job handle to wait for.
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+... any The results of the async function.
+.RE
 .TP
 .B sleep(seconds)
-Asynchronously pauses the task for the given duration. Must be used with \fBawait\fR.
+Pauses execution for a duration.
+.IP
+\fBParameters:\fR
+.RS
+\fIseconds\fR (number): The number of seconds to sleep.
+.br
+.RE
+.TP
+.B write(id, content)
+Buffers text for a specific output block.
+.IP
+\fBParameters:\fR
+.RS
+\fIid\fR (string): The block ID.
+.br
+\fIcontent\fR (string): The content to write.
+.br
+.RE
 .SH MODULES
 .SS cogeni
 .TP
-.B read_ast(path|handle, lang)
-Parses source into an AST table. If \fIpath\fR is relative, it resolves against \fB_CURRENT_FILE\fR. Automatically waits for files being updated by other tasks.
+.B cogeni.get_grammar(ext)
+Looks up a grammar name by file extension.
+.IP
+\fBParameters:\fR
+.RS
+\fIext\fR (string): The file extension (including dot).
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+string The grammar name.
+.RE
 .TP
-.B process(path)
-Triggers the full generation cycle on another file. Enables recursive orchestration.
+.B cogeni.outfile(id, path)
+Redirects write(id) to a file.
+.IP
+\fBParameters:\fR
+.RS
+\fIid\fR (string): The block ID.
+.br
+\fIpath\fR (string): The file path to overwrite.
+.br
+.RE
 .TP
-.B get_grammar(ext)
-Resolves grammar name from extension using the system configuration.
+.B cogeni.outtag(id, path, tag)
+Redirects write(id) to a specific tag in a file.
+.IP
+\fBParameters:\fR
+.RS
+\fIid\fR (string): The block ID.
+.br
+\fIpath\fR (string): The file path.
+.br
+\fItag\fR (string): The tag in the file.
+.br
+.RE
 .TP
-.B register_grammar(name, url, opts)
-Registers a custom grammar source for the current session.
+.B cogeni.process(path)
+Triggers the generation lifecycle for a file.
+.IP
+\fBParameters:\fR
+.RS
+\fIpath\fR (string): The file path.
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+boolean True on success.
+.RE
 .TP
-.B outfile(id, path)
-Redirects \fBwrite(id, ...)\fR to overwrite the \fBentire file\fR at \fIpath\fR.
+.B cogeni.read_ast(source, language)
+Parses source code into a detailed AST.
+.IP
+\fBParameters:\fR
+.RS
+\fIsource\fR (string|table): File path or handle.
+.br
+\fIlanguage\fR (string): The grammar name (e.g. "go", "python").
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+table The AST table.
+.RE
 .TP
-.B outtag(id, path, tag)
-Redirects \fBwrite(id, ...)\fR to a specific \fItag\fR in \fIpath\fR.
+.B cogeni.register_grammar(name, url, opts)
+Registers a custom grammar source URL.
+.IP
+\fBParameters:\fR
+.RS
+\fIname\fR (string): The grammar name.
+.br
+\fIurl\fR (string): The git repository URL.
+.br
+\fIopts\fR (table): Options (branch, build_cmd, artifact).
+.br
+.RE
 .SS fs
 .TP
-.B find(dir, opts)
-Recursively finds files. \fIopts\fR is a table: \fB{ type = "f"|"d", name = "glob", maxdepth = N }\fR.
+.B fs.basedir(path)
+Returns the directory of a path.
+.IP
+\fBParameters:\fR
+.RS
+\fIpath\fR (string): The file path.
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+string The directory part of the path.
+.RE
 .TP
-.B basedir(path), basename(path), join(...)
-Standard path utilities (directory name, file name, and joining).
-.SS jq / json
+.B fs.basename(path)
+Returns the last element of a path.
+.IP
+\fBParameters:\fR
+.RS
+\fIpath\fR (string): The file path.
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+string The file name.
+.RE
+.TP
+.B fs.find(dir, options)
+Recursively finds files and directories.
+.IP
+\fBParameters:\fR
+.RS
+\fIdir\fR (string): The starting directory.
+.br
+\fIoptions\fR (table): Filters (type="f"|"d", name="glob", maxdepth=N).
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+table A table where keys are paths and values are 'true'.
+.RE
+.TP
+.B fs.join(...)
+Joins multiple path elements.
+.IP
+\fBParameters:\fR
+.RS
+\fI...\fR (string): Path elements to join.
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+string The joined path.
+.RE
+.SS jq
 .TP
 .B jq.query(val, query)
-Executes a JQ query against any Lua value (usually an AST table). Returns a single value or a table of values.
+Executes a JQ query against a Lua value.
+.IP
+\fBParameters:\fR
+.RS
+\fIval\fR (any): The value to query (usually an AST table).
+.br
+\fIquery\fR (string): The JQ query string.
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+any|table A single result or a table of results, or nil.
+.RE
+.SS json
+.TP
+.B json.decode(str)
+Decodes a JSON string into a Lua table.
+.IP
+\fBParameters:\fR
+.RS
+\fIstr\fR (string): The JSON string.
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+any The decoded Lua value.
+.RE
 .TP
-.B json.encode(val), json.decode(str)
-Standard JSON serialization.
+.B json.encode(val, options)
+Encodes a Lua value to a JSON string.
+.IP
+\fBParameters:\fR
+.RS
+\fIval\fR (any): The value to encode.
+.br
+\fIoptions\fR (table): Optional formatting options (e.g. {indent=true}).
+.br
+.RE
+.IP
+\fBReturns:\fR
+.RS
+string The JSON string.
+.RE
 .SH EXAMPLES
 .SS 1. Basic Generation
 .nf
-# <cogeni>
-#   write("hello", "print('hello world')")
-# </cogeni>
-# <generated by="cogeni" id="hello">
-# </cogeni>
+    # <cogeni>
+    #   write("hello", "print('hello world')")
+    # </cogeni>
+    # <generated by="cogeni" id="hello">
+    # </cogeni>
 .fi
 .SS 2. Multi-file Orchestration
 .nf
--- cogeni.lua
-local files = fs.find(".", { type = "f", name = "*.py" })
-for path, _ in pairs(files) do
-    async(cogeni.process, path)
-end
+    -- cogeni.lua
+    local files = fs.find(".", { type = "f", name = "*.py" })
+    for path, _ in pairs(files) do
+        async(cogeni.process, path)
+    end
 .fi
 .SH AST TABLE SCHEMA
 Every node in the AST is a table:
diff --git a/src/lua_runtime/ast.go b/src/lua_runtime/ast.go
index 9bfc3cc..d217ea9 100644
--- a/src/lua_runtime/ast.go
+++ b/src/lua_runtime/ast.go
@@ -12,16 +12,15 @@ import (
 // cogeniReadAST parses source code into a detailed AST.
 // It is exposed to Lua as cogeni.read_ast(source, language).
 //
-// source: Can be a file path string or a file handle (table with a read() method).
-// language: The name of the Tree-sitter grammar to use (e.g., "python", "typescript").
-//
-// Path Resolution: If source is a relative path string, it is resolved against
-// the directory of the currently executing Lua script (_CURRENT_FILE).
-//
-// Dependency Management: If the file being read is also being updated by another
-// task in the same cogeni run, this call will block until that task is complete.
-//
-// Returns: (ast_table, nil) on success, or (nil, error_string) on failure.
+// <lua_api>
+// @module cogeni
+// @function read_ast
+// @summary Parses source code into a detailed AST.
+// @usage cogeni.read_ast(source, language)
+// @param source string|table File path or handle.
+// @param language string The grammar name (e.g. "go", "python").
+// @returns table The AST table.
+// </lua_api>
 func (rt *LuaRuntime) cogeniReadAST(L *lua.LState) int {
 	firstArg := L.CheckAny(1)
 	lang := L.CheckString(2)
@@ -102,6 +101,16 @@ func (rt *LuaRuntime) cogeniReadAST(L *lua.LState) int {

 // cogeniRegisterGrammar registers a custom grammar source URL.
 // Lua usage: cogeni.register_grammar("name", "url", { branch = "...", build_cmd = "..." })
+//
+// <lua_api>
+// @module cogeni
+// @function register_grammar
+// @summary Registers a custom grammar source URL.
+// @usage cogeni.register_grammar(name, url, opts)
+// @param name string The grammar name.
+// @param url string The git repository URL.
+// @param opts table Options (branch, build_cmd, artifact).
+// </lua_api>
 func (rt *LuaRuntime) cogeniRegisterGrammar(L *lua.LState) int {
 	name := L.CheckString(1)
 	url := L.CheckString(2)
@@ -131,6 +140,15 @@ func (rt *LuaRuntime) cogeniRegisterGrammar(L *lua.LState) int {

 // cogeniGetGrammar looks up a grammar name by file extension.
 // Lua usage: grammar = cogeni.get_grammar(".py")
+//
+// <lua_api>
+// @module cogeni
+// @function get_grammar
+// @summary Looks up a grammar name by file extension.
+// @usage cogeni.get_grammar(ext)
+// @param ext string The file extension (including dot).
+// @returns string The grammar name.
+// </lua_api>
 func (rt *LuaRuntime) cogeniGetGrammar(L *lua.LState) int {
 	ext := L.CheckString(1)
 	grammar := rt.cfg.GetGrammarForExtension(ext)
diff --git a/src/lua_runtime/fs.go b/src/lua_runtime/fs.go
index 5df2966..82a14bb 100644
--- a/src/lua_runtime/fs.go
+++ b/src/lua_runtime/fs.go
@@ -10,13 +10,15 @@ import (
 // fsFind recursively searches for files and directories matching specific criteria.
 // It is exposed to Lua as fs.find(dir, options).
 //
-// dir: The starting directory for the search.
-// options: A table containing optional filters:
-//   - type: "f" for files, "d" for directories.
-//   - name: Glob pattern for matching filenames (e.g., "*.lua").
-//   - maxdepth: Maximum recursion depth (integer).
-//
-// Returns: A table where keys are the relative paths found and values are 'true'.
+// <lua_api>
+// @module fs
+// @function find
+// @summary Recursively finds files and directories.
+// @usage fs.find(dir, options)
+// @param dir string The starting directory.
+// @param options table Filters (type="f"|"d", name="glob", maxdepth=N).
+// @returns table A table where keys are paths and values are 'true'.
+// </lua_api>
 func (rt *LuaRuntime) fsFind(L *lua.LState) int {
 	dir := L.CheckString(1)
 	options := L.OptTable(2, L.CreateTable(0, 0))
@@ -98,6 +100,15 @@ func (rt *LuaRuntime) fsFind(L *lua.LState) int {

 // fsBaseDir returns the directory of a path.
 // Lua usage: dir = fs.basedir("a/b/c.txt") -> "a/b"
+//
+// <lua_api>
+// @module fs
+// @function basedir
+// @summary Returns the directory of a path.
+// @usage fs.basedir(path)
+// @param path string The file path.
+// @returns string The directory part of the path.
+// </lua_api>
 func (rt *LuaRuntime) fsBaseDir(L *lua.LState) int {
 	path := L.CheckString(1)
 	L.Push(lua.LString(filepath.Dir(path)))
@@ -106,6 +117,15 @@ func (rt *LuaRuntime) fsBaseDir(L *lua.LState) int {

 // fsBaseName returns the last element of a path.
 // Lua usage: name = fs.basename("a/b/c.txt") -> "c.txt"
+//
+// <lua_api>
+// @module fs
+// @function basename
+// @summary Returns the last element of a path.
+// @usage fs.basename(path)
+// @param path string The file path.
+// @returns string The file name.
+// </lua_api>
 func (rt *LuaRuntime) fsBaseName(L *lua.LState) int {
 	path := L.CheckString(1)
 	L.Push(lua.LString(filepath.Base(path)))
@@ -114,6 +134,15 @@ func (rt *LuaRuntime) fsBaseName(L *lua.LState) int {

 // fsPathJoin joins multiple path elements.
 // Lua usage: path = fs.join("a", "b", "c") -> "a/b/c"
+//
+// <lua_api>
+// @module fs
+// @function join
+// @summary Joins multiple path elements.
+// @usage fs.join(...)
+// @param ... string Path elements to join.
+// @returns string The joined path.
+// </lua_api>
 func (rt *LuaRuntime) fsPathJoin(L *lua.LState) int {
 	top := L.GetTop()
 	parts := make([]string, top)
diff --git a/src/lua_runtime/jq.go b/src/lua_runtime/jq.go
index b9cd84e..54f1c77 100644
--- a/src/lua_runtime/jq.go
+++ b/src/lua_runtime/jq.go
@@ -10,11 +10,15 @@ import (
 // jqQuery executes a JQ query against a Lua value (typically an AST table).
 // It is exposed to Lua as jq.query(data, query_string).
 //
-// data: Any Lua value (table, string, number, etc.) to be queried.
-// query_string: A standard JQ query string (e.g., ".children[] | select(.type == 'class')").
-//
-// Returns: A single Lua value if the query has one result, or a table of values
-// if there are multiple results. Returns nil if no matches are found.
+// <lua_api>
+// @module jq
+// @function query
+// @summary Executes a JQ query against a Lua value.
+// @usage jq.query(val, query)
+// @param val any The value to query (usually an AST table).
+// @param query string The JQ query string.
+// @returns any|table A single result or a table of results, or nil.
+// </lua_api>
 func (rt *LuaRuntime) jqQuery(L *lua.LState) int {
 	value := L.CheckAny(1)
 	queryString := L.CheckString(2)
diff --git a/src/lua_runtime/json.go b/src/lua_runtime/json.go
index 010d127..01d72c5 100644
--- a/src/lua_runtime/json.go
+++ b/src/lua_runtime/json.go
@@ -10,6 +10,16 @@ import (
 // jsonEncode encodes a Lua value to a JSON string.
 // It supports an optional table for formatting options.
 // Lua usage: str = json.encode(data, { indent = true })
+//
+// <lua_api>
+// @module json
+// @function encode
+// @summary Encodes a Lua value to a JSON string.
+// @usage json.encode(val, options)
+// @param val any The value to encode.
+// @param options table Optional formatting options (e.g. {indent=true}).
+// @returns string The JSON string.
+// </lua_api>
 func (rt *LuaRuntime) jsonEncode(L *lua.LState) int {
 	lv := L.CheckAny(1)
 	goValue := luaValueToGoValue(lv)
@@ -43,6 +53,15 @@ func (rt *LuaRuntime) jsonEncode(L *lua.LState) int {

 // jsonDecode decodes a JSON string into a Lua table.
 // Lua usage: data = json.decode('{"foo": 1}')
+//
+// <lua_api>
+// @module json
+// @function decode
+// @summary Decodes a JSON string into a Lua table.
+// @usage json.decode(str)
+// @param str string The JSON string.
+// @returns any The decoded Lua value.
+// </lua_api>
 func (rt *LuaRuntime) jsonDecode(L *lua.LState) int {
 	jsonStr := L.CheckString(1)

diff --git a/src/lua_runtime/process.go b/src/lua_runtime/process.go
index 0e7c909..517e7d6 100644
--- a/src/lua_runtime/process.go
+++ b/src/lua_runtime/process.go
@@ -9,14 +9,14 @@ import (
 // cogeniProcess triggers the generation lifecycle for a specific file.
 // It is exposed to Lua as cogeni.process(path).
 //
-// path: The filesystem path of the file to process. If relative, it is resolved
-// against the directory of the current script.
-//
-// This function allows for recursive and multi-file orchestration. It invokes
-// the coordinator's processing logic, which may run the target file's own
-// <cogeni> blocks if they exist.
-//
-// Returns: true on success, or (nil, error_string) on failure.
+// <lua_api>
+// @module cogeni
+// @function process
+// @summary Triggers the generation lifecycle for a file.
+// @usage cogeni.process(path)
+// @param path string The file path.
+// @returns boolean True on success.
+// </lua_api>
 func (rt *LuaRuntime) cogeniProcess(L *lua.LState) int {
 	path := L.CheckString(1)
 	if rt.ProcessFunc == nil {
diff --git a/src/lua_runtime/runtime.go b/src/lua_runtime/runtime.go
index b619c83..c68dafd 100644
--- a/src/lua_runtime/runtime.go
+++ b/src/lua_runtime/runtime.go
@@ -50,6 +50,42 @@ type LuaRuntime struct {

 // New initializes a LuaRuntime, registers all custom modules (cogeni, fs, json, jq),
 // and prepares the AST parser.
+//
+// <lua_api>
+// @module global
+// @function write
+// @summary Buffers text for a specific output block.
+// @usage write(id, content)
+// @param id string The block ID.
+// @param content string The content to write.
+// </lua_api>
+//
+// <lua_api>
+// @module global
+// @function async
+// @summary Starts a new asynchronous task.
+// @usage async(fn, ...)
+// @param fn function The function to run asynchronously.
+// @param ... any Arguments to pass to the function.
+// @returns userdata A job handle.
+// </lua_api>
+//
+// <lua_api>
+// @module global
+// @function await
+// @summary Pauses until a job completes.
+// @usage await(job)
+// @param job userdata The job handle to wait for.
+// @returns ... any The results of the async function.
+// </lua_api>
+//
+// <lua_api>
+// @module global
+// @function sleep
+// @summary Pauses execution for a duration.
+// @usage sleep(seconds)
+// @param seconds number The number of seconds to sleep.
+// </lua_api>
 func New(cfg *config.Config) (*LuaRuntime, error) {
 	parser, err := astparser.New(cfg.Grammar.Location)
 	if err != nil {
@@ -176,6 +212,25 @@ func (rt *LuaRuntime) REPL() error {

 // registerCogeniModule exposes the 'cogeni' table to Lua.
 // Methods: read_ast, register_grammar, get_grammar, process, outfile, outtag.
+//
+// <lua_api>
+// @module cogeni
+// @function outfile
+// @summary Redirects write(id) to a file.
+// @usage cogeni.outfile(id, path)
+// @param id string The block ID.
+// @param path string The file path to overwrite.
+// </lua_api>
+//
+// <lua_api>
+// @module cogeni
+// @function outtag
+// @summary Redirects write(id) to a specific tag in a file.
+// @usage cogeni.outtag(id, path, tag)
+// @param id string The block ID.
+// @param path string The file path.
+// @param tag string The tag in the file.
+// </lua_api>
 func (rt *LuaRuntime) registerCogeniModule() {
 	cogeniTable := rt.L.CreateTable(0, 6)
 	rt.L.SetField(cogeniTable, "read_ast", rt.L.NewFunction(rt.cogeniReadAST))
